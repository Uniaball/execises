pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        jcenter() // 明确添加JCenter仓库
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        jcenter() // 添加JCenter仓库
        maven { url 'https://jitpack.io' } // 保留JitPack作为备用
    }
}

rootProject.name = "UniballProject"
include ':app'
